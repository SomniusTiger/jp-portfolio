// Logic for getting project images to parallax scroll

$(function() {

  // Get height of image and width of page
  var imageHeight = $('.project-main-image').height();
  var pageWidth = $(window).width();

});

